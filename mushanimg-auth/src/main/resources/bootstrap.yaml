spring:
  application:
    name: auth-service
  cloud:
    nacos:
      server-addr: 123.249.39.173:8848
      discovery:
        namespace: ${spring.profiles.active}
        group: mushanimg-project
      config:
        namespace: ${spring.profiles.active}
        group: mushanimg-project
        file-extension: yaml
        refresh-enabled: true
        shared-configs:
          - data-id: logging-${spring.profiles.active}
            group: mushanimg-common
            refresh: true
  profiles:
    active: dev